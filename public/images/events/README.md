# Event photos

Chapter photos for the Cursor Da Nang hero bento and past-event thumbnails.

Named `cursor-danang-meetup-*.webp`. Wire paths in `content/header-photos.ts` (and `content/events.ts` when real events are added).
